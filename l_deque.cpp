#include <bits/stdc++.h>
#define ll long long

using namespace std;

// X + Y = K
// Y = K - X
// 2X - (X + Y)
// ans = 2X - K

// 

inline auto solve () {
	int n; cin >> n;
	vector<int> a;
	for (int i = 0; i < n; i++) {
		int temp; cin >> temp;
		a.push_back(temp);
	}

}

int main () {
	int tc = 1; 
	cin >> tc;
	while (tc --> 0)
		solve();
}

